@page
@model ProyectoSaunaKalixto.Web.Pages.Egresos.CreateModel
@{
    ViewData["Title"] = "Nuevo Egreso";
    Layout = "_LayoutSidebar";
}

<div class="flex-1 overflow-auto">
    <div class="p-6">
        <div class="card">
            <div class="border-b px-6 py-4" style="border-color: var(--border);">
                <h2 class="text-lg font-semibold" style="color: var(--foreground);">Nuevo Egreso</h2>
                <p class="text-sm mt-1" style="color: var(--muted-foreground);">Registra un nuevo egreso en el sistema</p>
            </div>
            
            <div class="px-6 py-4">
                <partial name="_CreatePartial" model="this" />
            </div>
        </div>
    </div>
</div>
