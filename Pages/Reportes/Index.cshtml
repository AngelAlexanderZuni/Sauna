@page
@model ProyectoSaunaKalixto.Web.Pages.Reportes.IndexModel
@{
    ViewData["Title"] = "Reportes y Estadísticas";
    Layout = "_LayoutSidebar";
}

<div class="flex-1 overflow-auto">
    <div class="p-6 space-y-6">
        <div class="card">
            <div class="border-b px-6 py-4" style="border-color: var(--border);">
                <div class="flex items-center justify-between">
                    <div>
                        <h2 class="text-lg font-semibold" style="color: var(--foreground);">Reportes y Estadísticas</h2>
                        <p class="text-sm mt-1" style="color: var(--muted-foreground);">Resumen de inventario</p>
                    </div>
                    <div class="flex items-center space-x-2">
                        <a href="?handler=ExportInventario" class="inline-flex items-center px-4 py-2 text-sm font-medium rounded-md text-white transition-colors" style="background-color: var(--primary);">
                            <svg class="h-4 w-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                            </svg>
                            Exportar Inventario (CSV)
                        </a>
                    </div>
                </div>
            </div>

            <div class="px-6 py-6 space-y-6">
                <div class="grid grid-cols-1 md:grid-cols-5 gap-4">
                    <div class="p-4 rounded-lg border" style="border-color: var(--border); background: var(--card);">
                        <div class="text-xs" style="color: var(--muted-foreground);">Productos</div>
                        <div class="text-2xl font-bold" style="color: var(--foreground);">@Model.TotalProductos</div>
                        <div class="text-xs" style="color: var(--muted-foreground);">@Model.UnidadesTotales unidades</div>
                    </div>
                    <div class="p-4 rounded-lg border" style="border-color: var(--border); background: var(--card);">
                        <div class="text-xs" style="color: var(--muted-foreground);">Valor Total</div>
                        <div class="text-2xl font-bold" style="color: var(--foreground);">S/ @Model.ValorTotalInventario.ToString("N2")</div>
                        <div class="text-xs" style="color: var(--muted-foreground);">Costo: S/ @Model.ValorTotalCosto.ToString("N2")</div>
                    </div>
                    <div class="p-4 rounded-lg border" style="border-color: var(--border); background: var(--card);">
                        <div class="text-xs" style="color: var(--muted-foreground);">Stock Bajo</div>
                        <div class="text-2xl font-bold" style="color: var(--foreground);">@Model.ProductosStockBajo</div>
                    </div>
                    <div class="p-4 rounded-lg border" style="border-color: var(--border); background: var(--card);">
                        <div class="text-xs" style="color: var(--muted-foreground);">Sin Stock</div>
                        <div class="text-2xl font-bold" style="color: var(--foreground);">@Model.ProductosSinStock</div>
                    </div>
                    <div class="p-4 rounded-lg border" style="border-color: var(--border); background: var(--card);">
                        <div class="text-xs" style="color: var(--muted-foreground);">Margen Estimado</div>
                        <div class="text-2xl font-bold" style="color: var(--foreground);">S/ @(Model.ValorTotalInventario - Model.ValorTotalCosto).ToString("N2")</div>
                    </div>
                </div>

                <div class="rounded-lg border" style="border-color: var(--border); background: var(--card);">
                    <div class="px-4 py-3 border-b" style="border-color: var(--border);">
                        <h3 class="text-sm font-semibold" style="color: var(--foreground);">Top 10 productos por movimientos</h3>
                    </div>
                    <div class="table-scroll" style="max-height: 420px; overflow-y:auto;">
                        <table class="w-full">
                            <thead class="border-b" style="border-color: var(--border); background-color: var(--muted); position: sticky; top:0;">
                                <tr>
                                    <th class="px-4 py-2 text-left text-xs font-medium" style="color: var(--muted-foreground);">Producto</th>
                                    <th class="px-4 py-2 text-right text-xs font-medium" style="color: var(--muted-foreground);">Cantidad Movida</th>
                                    <th class="px-4 py-2 text-right text-xs font-medium" style="color: var(--muted-foreground);">Ingresos Estimados</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y" style="border-color: var(--border);">
                                @foreach (var t in Model.TopProductos)
                                {
                                    <tr>
                                        <td class="px-4 py-2" style="color: var(--foreground);">@t.Nombre</td>
                                        <td class="px-4 py-2 text-right" style="color: var(--foreground);">@t.CantidadMovida</td>
                                        <td class="px-4 py-2 text-right" style="color: var(--foreground);">S/ @t.IngresosEstimados.ToString("N2")</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
